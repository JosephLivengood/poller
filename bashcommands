sass --watch public/css
mongod --smallfiles (redundant, but needed to run in c9 enviroment)
mongo (obv)
node server.js

//Optional, we will be rendering as temp in express, not sendfileing it
pug public -P
//But not really, now that most pugs use recursives based on render data from express
//(so youll get an error)
